<% provide(:title, 'Все дела в суде') %>
<div class="row">
      <%= render './courts/menu' %>
</div>
<div class="row">
  <div class="span12">

	<%= will_paginate %>
	<div class="bs-docs-example">
		<table class="table table-hover">
			<thead><tr><th>По договору №</th><th>Организация</th><th>Дата подачи иска</th><th>Сумма иска</th><th>Статус дела</th>
				  <% if current_user.admin? %>
				<th>Действие</th>
				  <% end %>
			</tr></thead>
			<tdoby>
  			 <%= render @delos %>
			</tbody>
		</table></div>
	<%= will_paginate %> 


      <%= render './shared/modelstatus' %>


  </div>
</div>