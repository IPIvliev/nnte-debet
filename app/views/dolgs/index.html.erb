<% provide(:title, 'Все договора') %>
<div class="row">
      <%= render './finances/menu' %>
  <div class="span8">

	<%= will_paginate %>
		<table class="table table-bordered table-striped">
			<thead><tr><th>Организация</th><th>Номер договора</th><th>Сумма</th><th>Дата</th>
				  <% if current_user.admin? %>
				<th>Действие</th>
				  <% end %>
			</tr></thead>
			<tdoby>
  			 <%= render @dolgs %>
			</tbody>
		</table>
	<%= will_paginate %> 

  </div>
</div>