<% provide(:title, 'All users') %>
<div class="row">
      <%= render 'menu' %>
  <div class="span8">

<ul class="breadcrumb">
  <li><a href="#">Главная</a> <span class="divider">/</span></li>
  <li class="active">Все организации</li>
</ul>

<%= form_tag agents_path, :method => 'get', :id => "agents_search" do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>

		<table class="table table-striped">
			<thead><tr><th><%= sortable "name", "Название" %></th><th>Адресс</th><th>Телефон</th><th>Сумма долга</th>
				  <% if current_user.admin? %>
				<th>Действие</th>
				  <% end %>
			</tr></thead>
			<tdoby>

				<%= render @agents %>
			</tbody>
		</table>
<%= will_paginate @agents %>
  </div>
</div>